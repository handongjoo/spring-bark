<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="egovframework.example.auth.service.AuthMapper">

	<!-- 아이디로 유저 확인 -->
	<select id="getUserById" parameterType="String" resultType="AuthVo">
		SELECT
		user_id, password, nickname
		WEHRE user_id = #{userId}
	</select>
	
	<select id="getUserByNick" parameterType="String" resultType="String">
		SELECT
		nickname
		WEHRE nickname = #{nickname}
	</select>
	
	<!-- 유저 생성(회원가입) -->
	<insert id="createUser" parameterType="AuthVo">
		INSERT INTO users
		(user_id, password, nickname)
		VALUES
		(#{userId}, #{password}, #{nickname})
	</insert>
	
	<!-- 유저 정보 수정 -->
	<update id="updateUser" parameterType="String">
		UPDATE users
		SET password = #{password}, nickname = #{nickname}
		WHERE user_id = #{userId}
	</update>
	
	<!-- 유저 삭제 -->
	<delete id="deleteUser" parameterType="AuthVo">
		DELETE FROM users
		WHERE user_id = #{userId}
		AND password = #{password}
	</delete>

</mapper>